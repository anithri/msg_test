$('#statusMessages').append("<%= j (render partial: 'layouts/messages', layout: false) %>");
$("#statusMessages .alert").last().delay(5000).fadeTo(500,0).slideUp(500,function(){$(this.remove())});
<%- flash.discard %>
